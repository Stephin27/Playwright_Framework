{
  "name": "playwright-framework",
  "version": "1.0.0",
  "description": "Playwright Automation Framework with POM and Allure Reporting",
  "main": "index.js",
  "scripts": {
    "test": "playwright test && npm run report:single || npm run report:single",
    "report": "allure generate allure-results -o allure-report --clean && allure open allure-report",
    "report:single": "allure generate allure-results -o allure-report --clean && allure-combine allure-report"
  },
  "keywords": [
    "playwright",
    "automation",
    "typescript",
    "pom",
    "allure"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "allure-commandline": "^2.25.0",
    "allure-playwright": "^2.10.0",
    "playwright": "^1.40.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
